type Query {
  person: Person!
}

type Person {
  name: String!
  age: Int!
  address: Address!
}

type Address {
  number: String!
  street: String!
  city: String!
}

type Mutation {
  setName(name: String!): Person!
}

type Subscription {
  messages: Int!
}
