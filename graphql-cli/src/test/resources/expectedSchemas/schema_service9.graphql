type Query {
  isHoliday(weekday: Weekday): Boolean!
  getDay(isHoliday: Boolean!): [Weekday!]!
  sendEmail(message: String!): String!
}

enum Weekday {
  SATURDAY
  FRIDAY
  THURSDAY
  WEDNESDAY
  TUESDAY
  MONDAY
  SUNDAY
}
