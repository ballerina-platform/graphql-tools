type Query {
    "Fetch all the books from database"
    books: [Book]
    "Fetch a book by its id"
    book(
        "The id of the book to fetch"
        id: Int!
    ): Book
}

type Book {
    id: Int!
    title: String!
}
