type Query {
    """
    Fetch all the books from database
    """
    books: [Book]
    """
    Fetch a book by its id
    id can't be null
    id is an integer
    """
    book(
        id: Int!
    ): Book
}

type Book {
    id: Int!
    title: String!
}
