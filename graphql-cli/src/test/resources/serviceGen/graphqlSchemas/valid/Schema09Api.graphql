type Query {
    student(id: Int!): Student
}

interface Info {
    name: String!
}

type Book {
    name: String!
}

type Student implements Info {
    id: Int!
    name: String!
}

type Teacher implements Info {
    id: Int!
    name: String!
}