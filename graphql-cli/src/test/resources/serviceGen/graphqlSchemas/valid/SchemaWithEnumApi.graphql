type Query {
    student(id: Int!): Student
}

type Student {
    id: Int!
    name: String!
    gender: Gender!
    status: Status
}

enum Gender {
    MALE
    FEMALE
}

enum Status {
    ONLINE
    OFFLINE
}
