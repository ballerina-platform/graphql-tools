type Query {
    book(id: Int!, title: String): Book
    books: [Book]
    authors: [Author!]!
}

type Subscription {
    bookTitles: String!
}

type Author {
    id: Int!
    name: String!
}

type Book {
    id: Int!
    title: String!
}
