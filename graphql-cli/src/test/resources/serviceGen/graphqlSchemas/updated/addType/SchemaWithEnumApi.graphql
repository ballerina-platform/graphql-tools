type Query {
    student(id: Int!): Student
}

type Student {
    id: Int!
    name: String!
}

enum Gender {
    MALE
    FEMALE
}

type Author {
    id: Int!
    name: String!
}

enum Category {
    JUNIOR
    SENIOR
}
