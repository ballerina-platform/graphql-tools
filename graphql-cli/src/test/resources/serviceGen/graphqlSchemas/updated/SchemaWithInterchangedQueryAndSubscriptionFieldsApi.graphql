type Query {
    book(id: Int!, title: String): Book
    authors: [Author!]!
}

type Subscription {
    bookTitles: String!
    authorNames: String!
}

type Author {
    id: Int!
    name: String!
}

type Book {
    id: Int!
    title: String!
}
