type Query {
    studentInfo(id: Int!): Info
}

interface Info {
    name: String!
    age: Int!
}

type Book {
    name: String!
}

type Student implements Info {
    id: Int!
    name: String!
    age: Int!
}

type Teacher implements Info {
    id: Int!
    name: String!
    age: Int!
}
